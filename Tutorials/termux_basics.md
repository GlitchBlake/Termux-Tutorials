# Basic Things To Do in Termux

## Update packages
It is recommended to update the packages to keep Termux synced. To update the packages you can run this simple command:

* `pkg update && pkg upgrade`
